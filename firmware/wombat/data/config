; Comments are introduced by semi-colons
# or hash symbols.

# Leading and trailing white-space are stripped from lines,
# blank lines are ignored.
#
# To update run:
# pio run -t uploadfs

interval measure 900
interval uplink 3600

mqtt topic wombat_`node_id`

sensor edge on

mqtt host sherman-pablo.bnr.la
mqtt port 1833
mqtt user broker
mqtt password Laurel_stonefish_mica3!